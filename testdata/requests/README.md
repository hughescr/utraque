# Request fixtures

Each fixture is named `<case>.<protocol>.<role>.<ext>`, where protocol is `anthropic` or `responses` and role is `input` or `golden`. Request inputs use
`<case>.anthropic.input.json`; translated Responses API goldens use
`<case>.responses.golden.json`.
